# راهنمای سیستم تم MarkazTech

## 📁 فایل‌های مربوط به تم

### 1. `src/app/globals.css`
- **نقش**: تعریف متغیرهای CSS برای تم‌های روشن و تاریک
- **محتوا**: همه رنگ‌ها و متغیرهای تم

### 2. `tailwind.config.js`
- **نقش**: اتصال متغیرهای CSS به کلاس‌های Tailwind
- **محتوا**: تنظیمات Tailwind برای استفاده از متغیرها

## 🎨 کلاس‌های آماده

### رنگ‌های پس‌زمینه
```jsx
bg-background          // پس‌زمینه اصلی
bg-background-secondary // پس‌زمینه ثانویه
bg-card               // پس‌زمینه کارت‌ها
bg-primary            // رنگ اصلی برند
bg-secondary          // رنگ ثانویه
```

### رنگ‌های متن
```jsx
text-foreground           // متن اصلی
text-foreground-secondary // متن ثانویه
text-foreground-muted     // متن کم‌اهمیت
text-primary             // متن با رنگ برند
text-card-foreground     // متن روی کارت
```

### کلاس‌های ترکیبی آماده
```jsx
card-base        // کارت کامل با پس‌زمینه، متن و حاشیه
btn-primary      // دکمه اصلی
btn-secondary    // دکمه ثانویه
text-muted       // متن کم‌اهمیت
text-secondary   // متن ثانویه
bg-page          // پس‌زمینه صفحه
```

## 🔧 نحوه تغییر رنگ‌ها

### تغییر رنگ اصلی برند
در فایل `globals.css`:
```css
:root {
  --primary: 221.2 83.2% 53.3%;      /* آبی فعلی */
  /* تغییر به قرمز: --primary: 0 84.2% 60.2%; */
  /* تغییر به سبز: --primary: 142.1 76.2% 36.3%; */
}

.dark {
  --primary: 217.2 91.2% 59.8%;      /* آبی روشن در تم تاریک */
}
```

### تغییر پس‌زمینه اصلی
```css
:root {
  --background: 0 0% 100%;           /* سفید فعلی */
  /* تغییر به خاکستری روشن: --background: 210 40% 98%; */
}

.dark {
  --background: 222.2 84% 4.9%;      /* مشکی فعلی */
  /* تغییر به خاکستری تیره: --background: 217.2 32.6% 17.5%; */
}
```

## 📋 مثال‌های کاربردی

### کارت محصول
```jsx
<div className="card-base p-4 rounded-xl shadow-theme">
  <h3 className="text-foreground font-bold">عنوان محصول</h3>
  <p className="text-foreground-muted">توضیحات</p>
  <button className="btn-primary">خرید</button>
</div>
```

### صفحه با پس‌زمینه ثانویه
```jsx
<div className="min-h-screen bg-background-secondary">
  <div className="container mx-auto px-4">
    <h1 className="text-foreground">عنوان صفحه</h1>
  </div>
</div>
```

## 🎯 نکات مهم

1. **همیشه از متغیرها استفاده کن**: به جای `bg-blue-500` از `bg-primary` استفاده کن
2. **کلاس‌های آماده**: از کلاس‌های `btn-primary`, `card-base` استفاده کن
3. **سازگاری**: همه کلاس‌ها با هر دو تم سازگار هستند
4. **تست**: بعد از تغییر، هر دو تم رو تست کن

## 🚀 اضافه کردن رنگ جدید

### 1. در globals.css
```css
:root {
  --accent: 32.2 95% 44.1%;          /* نارنجی */
  --accent-foreground: 355.7 100% 97.3%;
}

.dark {
  --accent: 32.2 95% 54.1%;          /* نارنجی روشن‌تر */
  --accent-foreground: 20.9 95.3% 90.1%;
}
```

### 2. در tailwind.config.js
```js
colors: {
  accent: {
    DEFAULT: "hsl(var(--accent))",
    foreground: "hsl(var(--accent-foreground))",
  },
}
```

### 3. استفاده در کامپوننت
```jsx
<button className="bg-accent text-accent-foreground">
  دکمه با رنگ جدید
</button>
```